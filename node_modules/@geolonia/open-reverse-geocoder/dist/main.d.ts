export interface ReverseGeocodingResult {
    code: string;
    prefecture: string;
    city: string;
}
declare type LngLat = [number, number];
export interface ReverseGeocodingOptions {
    /** どのズームを使うか。デフォルトは 10 */
    zoomBase: number;
    /** タイルが入ってるURLフォーマット。 */
    tileUrl: string;
    layer: string;
}
export declare const openReverseGeocoder: (input: LngLat, options?: Partial<ReverseGeocodingOptions>) => Promise<ReverseGeocodingResult>;
export {};
